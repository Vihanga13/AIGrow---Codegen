# Netlify build & deploy config.
# Lets Netlify build the site itself (recommended) instead of drag-and-drop.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
